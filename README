Django Based Backend for E-Commerce Website

Features
-Defined categories for orders
-Seperate user login for superuser\admin and for customers
-Place orders and edit order details efficiently
-Payment Gateway using BrainTree

How to install
-Just hit  pip install -r /path/to/requirements.txt  (it will  install all the requirements)
-Then use python manage.py runserver to go the backend page
